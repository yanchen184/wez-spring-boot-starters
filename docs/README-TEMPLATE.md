# {模組名稱}

{一句話描述} — {關鍵功能 A}、{關鍵功能 B}、{關鍵功能 C}

---

## 加入後你的專案自動獲得

<!-- 核心賣點表格：讓讀者 3 秒內決定要不要用 -->

| 功能 | 說明 |
|------|------|
| ... | ... |

---

## 快速開始

### 1. 引入依賴

```xml
<dependency>
    <groupId>com.company.common</groupId>
    <artifactId>{artifactId}</artifactId>
</dependency>
```

### 2. 最小使用範例

```java
// 最簡單的使用方式，讓人 copy-paste 就能跑
```

### 3. 完成

不需要任何配置。

---

## 功能總覽

<!-- 所有功能的清單，每項一句話 -->

- **功能 A** — 說明
- **功能 B** — 說明
- **功能 C** — 說明

---

## 核心 API

<!-- 主要的 public class / method，搭配程式碼範例 -->

### {主要 Class}

```java
// 使用範例
```

| 方法 | 說明 |
|------|------|
| `method()` | ... |

---

## 配置

<!-- 完整的 YAML 配置，含每個屬性的預設值和說明 -->

```yaml
common:
  {module}:
    enabled: true                 # 是否啟用（預設 true）
    # ... 其他配置
```

---

## 設計決策

<!-- 為什麼這樣做、為什麼不做某些事 -->

### 要什麼

| 決策 | 原因 |
|------|------|
| ... | ... |

### 不要什麼

| 決策 | 原因 |
|------|------|
| ... | ... |

---

## 依賴關係

<!-- 此模組與其他內部模組的關係 -->

```
{module}
├── common-xxx-spring-boot-starter   ← 說明
└── common-yyy-spring-boot-starter   ← 說明
```

---

## 專案結構與技術規格

```
{module}/
├── src/main/java/com/company/common/{pkg}/
│   ├── config/          # AutoConfiguration + Properties
│   ├── service/         # 核心 Service
│   └── ...
└── pom.xml
```

| 項目 | 值 |
|------|-----|
| Java | 21 |
| Spring Boot | 4.0.3 |
| 自動配置 | `AutoConfiguration.imports` |
| ... | ... |

---

## 版本

- 1.0.0 — {初始版本說明}

<!--
==========================================================
README 章節順序指南（供撰寫者參考，不會顯示在 GitHub）
==========================================================

順序邏輯：讀者越懶的章節放越前面

1. 標題 + 一句話描述      → 這是什麼？
2. 加入後自動獲得          → 為什麼要用？（3 秒說服）
3. 快速開始               → 怎麼用？（最小可執行範例）
4. 功能總覽               → 還有什麼？（完整功能清單）
5. 核心 API               → 怎麼用進階功能？
6. 配置                   → 怎麼調整？
7. 設計決策               → 為什麼這樣做？（要什麼 / 不要什麼）
8. 依賴關係               → 跟其他模組的關係？
9. 專案結構與技術規格      → 程式碼在哪 + 技術查表
10. 版本                  → 變更紀錄

適用規則：
- 不是每個章節都必須有，視模組大小決定
  - 完整 Starter（log、response、security）→ 全部章節
  - 子模組（report-core、引擎）→ 省略「加入後自動獲得」，著重 API + 使用範例
  - 空殼 Starter（report-starter）→ 只需 1-3 節
  - 工具模組（build-tools）→ 省略「快速開始」，著重規則說明
- 長 README（> 200 行）在標題後加「## 目錄」
- 配置章節必須是完整可 copy 的 YAML，含每個屬性的預設值
- 程式碼範例必須可直接 copy-paste 執行
-->
